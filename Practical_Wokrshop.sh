uname -a //Shows system info
which gpg
gpg --version //Shows GPG version info
gpg --armor --export 
gpg --list-secret-keys --keid-format=long //Shows the lists of the private keys
gpg --armor --export-secret-keys XXXXXX // Exports the secret keys