#!/bin/bash -x
# UNIX COMMAND DOCUMENTATION
# bash --version : Displays the installed GNU Bash shell version and build details.
# env            : Lists all active global environment variables (e.g., SHELL, LANG, PWD).
# echo ${RANDOM} : Returns a pseudo-random integer between 0 and 32767.
# echo ${OSTYPE} : Identifies the current operating system environment (e.g., linux-gnu).
# ps -e -f       : Lists all active processes (-e) with full details (-f) like PID and PPID.
# ps -ef         : Identical to 'ps -e -f' (lists all active processes in full format).
# df --human-readable : Shows disk space usage of all filesystems in readable units (GB, MB).
# bash --version : Displays the installed GNU Bash shell version and build details.